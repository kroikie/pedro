import 'package:flutter/material.dart';

class GameSummary extends StatelessWidget {
  final String gameId;

  const GameSummary({super.key, required this.gameId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
